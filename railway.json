{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd heydoctor-backend-pro && npm ci && rm -rf dist && npm run build"
  },
  "deploy": {
    "startCommand": "cd heydoctor-backend-pro && node dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
